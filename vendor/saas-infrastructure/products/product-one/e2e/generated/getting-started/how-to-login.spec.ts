import { test, expect } from '@playwright/test';
// Guide-based test: How to Login
// Source: /guides/getting-started/how-to-login
// Priority: critical
// Roles: user

test.describe('Getting Started - How to Login', () => {
  test('should how to login @critical @smoke @auth', async ({ page }) => {
  // 1. Navigate to the login page at /login
  // TODO: Add action - Navigate to the login page at /login
  // await page.getByRole('button', { name: '...' }).click();

  // 2. Enter your email address in the email field
  // TODO: Add action - Enter your email address in the email field
  // await page.getByRole('button', { name: '...' }).click();

  // 3. Enter your password in the password field
  // TODO: Add action - Enter your password in the password field
  // await page.getByRole('button', { name: '...' }).click();

  // 4. Click the "Log In" button
  // TODO: Add action - Click the "Log In" button
  // await page.getByRole('button', { name: '...' }).click();

  // 5. Verify you are redirected to the dashboard
  // TODO: Add assertion - Verify you are redirected to the dashboard
  // await expect(page.getByText('...')).toBeVisible();

  // 6. Check that your name or email appears in the user menu
  // TODO: Add assertion - Check that your name or email appears in the user menu
  // await expect(page.getByText('...')).toBeVisible();
  });
});
