project_id = "REPLACE_WITH_DEV_PROJECT_REF"

[auth]
site_url = "http://localhost:8080"
additional_redirect_urls = ["http://localhost:8080"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true

[functions.send-league-invite]
verify_jwt = false

[functions.generate-pairings]
verify_jwt = false
